name: Newman Run

on:
  workflow_dispatch:

  pull_request:
    branches:
      - master

jobs:
  newman:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: matt-ball/newman-action@master
        with:
          collection: Integration Testes API.postman_collection.json
